version: "3"

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    volumes:
      - ./logs:/app/logs

  frontend:
    build: ./frontend
    ports:
      - "8080:80"

  db:
    image: mongo
    ports:
      - "27017:27017"
